import { newAccount } from "@/src/services/financial";

export default async function NewAccount() {


    if( null == null ) {
        return (<>No Such Account</>);
    }

    return (<>Hello world</>)
}